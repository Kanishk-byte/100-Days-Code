#include <bits/stdc++.h>
#include <cmath>
#include <algorithm>
#include <vector>
#include <string>

using namespace std;

// * Ques : Vector Basics (Replacement of Array)

int main()
{

    vector<int> v; // You not need to mention size.

    // * Inserting Element's
    v.push_back(6);
    v.push_back(1);
    v.push_back(9);
    v.push_back(0);

    // * Printing Element's
    cout << v[0] << " ";
    cout << v[1] << " ";
    cout << v[2] << " ";
    cout << v[3] << " ";

    return 0;
}