#include <bits/stdc++.h>
#include <cmath>
#include <algorithm>
#include <vector>
#include <string>

using namespace std;

// * Ques : Size of Array

int main()
{

    int arr[] = {1, 4, 5, 6, 9, 7, 5, 2, 3};
    int size = sizeof(arr) / sizeof(arr[0]);

    cout << "Size of Array : " << size;

    return 0;
}