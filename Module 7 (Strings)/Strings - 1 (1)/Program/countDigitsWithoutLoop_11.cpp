#include <bits/stdc++.h>
#include <cmath>
#include <algorithm>
#include <vector>
#include <string>

using namespace std;

// * Ques : Count the no. of digits without using any loop.

int main()
{

    int num = 88755;
    string str = to_string(num);
    cout << str.length() << endl;

    return 0;
}