#include <bits/stdc++.h>
#include <cmath>
#include <algorithm>
#include <vector>
#include <string>

using namespace std;

// * Ques : Predict the output

int main()
{

    int x = 10, y = 0;
    while (x >= y)
    {
        x--;
        y++;
        cout << x << " " << y << endl;
    }

    return 0;
}