#include <bits/stdc++.h>
#include <cmath>
#include <algorithm>
#include <vector>
#include <string>

using namespace std;

// * Ques : int *p, q;
/*
p is a pointer and q is an integer.
p and q both are pointers.
P and q both are integers.
Syntax is incorrect.
*/

int main()
{
    cout << "p is a pointer and q is an integer.";
    return 0;
}