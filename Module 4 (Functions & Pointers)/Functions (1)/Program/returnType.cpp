#include <bits/stdc++.h>
#include <cmath>
#include <algorithm>
#include <vector>
#include <string>

using namespace std;

// * Ques : Return Type
// * x and y are arguments of the function.

int sum(int x, int y) // * Formal Arguments
{
    return x + y;
}

int main()
{

    cout << sum(4, 6); // * Actual Arguments

    return 0;
}