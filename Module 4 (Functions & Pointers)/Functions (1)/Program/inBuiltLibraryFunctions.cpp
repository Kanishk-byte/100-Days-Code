#include <bits/stdc++.h>
#include <cmath>
#include <algorithm>
#include <vector>
#include <string>

using namespace std;

// * Ques : In - built library functions.
/*
 * min(x,y) -> This function will give minimum between x and y.
 * max(x,y) -> This function will give maximum between x and y.
 * sqrt(x,y) -> This function will give square root of x.
 */

int main()
{

    // * Minimum
    cout << min(4, 6) << endl;

    // * Maximum
    cout << max(4, 6) << endl;

    // * Square Root
    cout << sqrt(49) << endl;

    return 0;
}