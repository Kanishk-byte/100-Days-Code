#include <bits/stdc++.h>
#include <cmath>
#include <algorithm>
#include <vector>
#include <string>

using namespace std;

// * Ques : State True and False :->

int main()
{

    cout << "A function may be called more than once from any other function : " << "True" << endl;
    cout << "It is necessary for a function to return some value : " << "True, not necessary for void" << endl;

    return 0;
}