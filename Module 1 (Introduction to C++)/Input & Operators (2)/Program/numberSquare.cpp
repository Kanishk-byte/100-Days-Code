#include <bits/stdc++.h>
#include <cmath>
#include <algorithm>
#include <vector>
#include <string>
using namespace std;

// * Print the square of the number

int main()
{

    int x;

    cout << "Enter x : ";
    cin >> x; // * User will give 'x' a value.

    cout << "Square is : " << (x * x);

    return 0;
}