#include <bits/stdc++.h>
#include <cmath>
#include <algorithm>
#include <vector>
#include <string>
using namespace std;

// * Print the output of the following code fragment.
// * Precedence will be Right to Left

int main()
{

    int num1;
    int p = 5, q = 10;

    p += q -= p;
    cout << p << " " << q << endl;

    return 0;
}