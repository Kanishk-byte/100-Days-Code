#include <bits/stdc++.h>
#include <cmath>
#include <algorithm>
#include <vector>
#include <string>
using namespace std;

// * Predict the output of the following code.

int main()
{

    int p, q;
    cout << "Enter the values of p & q : ";
    cin >> p >> q;

    cout << "p = " << p << " q = " << q;

    return 0;
}