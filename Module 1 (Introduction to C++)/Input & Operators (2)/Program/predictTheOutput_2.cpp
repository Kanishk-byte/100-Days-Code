#include <bits/stdc++.h>
#include <cmath>
#include <algorithm>
#include <vector>
#include <string>
using namespace std;

// * Predice the output of the following code fragment.

int main()
{

    bool p = false;
    bool q = false;
    bool r = true;

    cout << (p == q == r);

    return 0;
}