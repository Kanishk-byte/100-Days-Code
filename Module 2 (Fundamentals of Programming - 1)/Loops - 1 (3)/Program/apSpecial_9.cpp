#include <bits/stdc++.h>
#include <cmath>
#include <algorithm>
#include <vector>
#include <string>

using namespace std;

// * Ques : Print the AP (100,97,94,91,88, . . . [Positive Integer])

int main()
{

    for (int a = 100; a > 0; a -= 3)
    {
        cout << a << endl;
    }

    return 0;
}