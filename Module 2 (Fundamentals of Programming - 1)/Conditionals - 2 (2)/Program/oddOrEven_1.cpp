#include <bits/stdc++.h>
#include <cmath>
#include <algorithm>
#include <vector>
#include <string>
using namespace std;

// * Odd or Even using Ternary Operator.

int main()
{

    int num;
    cout << "Enter number : ";
    cin >> num;

    (num % 2 == 0) ? cout << "Even Number" : cout << "Odd Number";

    return 0;
}
