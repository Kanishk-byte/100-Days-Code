#include <bits/stdc++.h>
#include <cmath>
#include <algorithm>
#include <vector>
#include <string>
using namespace std;

// * Enter the marks and decide if he is pass or fail

int main()
{

    int _marks;
    cout << "Enter the marks : ";
    cin >> _marks;

    (_marks > 33) ? cout << "Pass" : cout << "Fail";

    return 0;
}